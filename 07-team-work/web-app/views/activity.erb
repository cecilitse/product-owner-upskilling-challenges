<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">

    <title>Team Building - Activity 1</title>
    <%= stylesheet '/style.css' %>
    <script src="https://kit.fontawesome.com/57e59f05ad.js" crossorigin="anonymous"></script>
  </head>
  <body>
    <div class="banner" style="background-image: linear-gradient(0deg, rgba(0,0,0,0.9) 0%, rgba(0,0,0,0.4) 80%), url('<%= @activity["photo_url"] %>')";>
      <div class="container container-head">
        <span class="category-detail"><%= @activity["category"].upcase %></span>
        <h1><%= @activity["name"] %></h1>
        <p><%= @descriptions[rand(10)]["short"] %></p>
      </div>
    </div>

    <div class="container container-main">
      <div class="container-activity">
        <div class="content-left">
          <a class="breadcrumbs" href="../"> < Back</a>
          <h2>Description of the activity</h2>
          <p><%= @descriptions[rand(10)]["long"] %></p>
          <h2>What Kanye says</h2>
          <p class="quote"><%= @quote%></p>
          <h2>Pictures</h2>


        </div>
        <div class="content-right">
          <div class="price-box">
            <p class="price-detail"><%= @activity["price"] %>€</p>
            <p class="participants-detail">for <%= @activity["participants_count"] %> participants</p>

          </div>
          <p class="city-detail"><strong>🌍</strong> <%=  @activity["city"] %></p>
          <p class="duration-detail"><strong>🕐</strong> <%= @activity["duration"] %> minutes</p>
          <a href="" class="btn btn-book">Book activity</a>
          <a href="" class="btn btn-info">Ask for information</a>
          <p class="wishlist"><3 Wishlist</p>
          <p class="wishlist-line">Add to wishlist for:</p>
          <form method="POST" action="/activities/<%= @activity["id"] %>/wishes">

          <select name="add_team">
            <option value="">Choose a team</option>
            <% @teams.each do |team| %>
            <option value="<%= team["id"] %>"><%= team["name"] %></option>
            <% end %>
          </select>
          <input type="submit" value="OK">
          </form>
          <p class="wishlist-line">Wishlisted by XX teams:</p>

          <div class="wishlist-teams">
            <% @current_activity_teams_wishes.each do |team| %>
            <div class="wishlist-team"><%= team["name"] %><a href="" class="delete-wish">X</a></div>
            <% end %>
          </div>

        </div>
      </div>
    </div>

    <div class="footer">

    </div>

  </body>
</html>
