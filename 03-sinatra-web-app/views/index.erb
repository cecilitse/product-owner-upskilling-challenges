<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">

    <title>Team Building - Activities</title>
    <%= stylesheet '/style.css' %>
  </head>
  <body>
    <h1>Activities</h1>
      <% @activities.each do |activity| %>
      <p>
        <%= activity["name"] %> [<%=activity["category"].upcase %>]
        Located in <%= activity["city"] %>
        Duration of <%= activity["duration"] %> minutes
        For <%= activity["participants"] %> participants
        Charged <%= activity["price"] %> euros
        </p>
        <div class="activity-card">
          <div class="header">
            <h2 title="Giant Cluedo"><%= activity["name"] %></h2>
            <span class="label label-primary category"><%=activity["category"].upcase %></span>
          </div>
          <div class="picture" style="background-image: url('https://www.voyage-insolite.com/files/2016/07/la-grand-%C3%A9l%C3%A9phant-nantes.jpg')"></div>
          <div class="activity-details">
            <div class="sub-header">
              <span class="city"><%= activity["city"] %></span>
              <span class="price">100 euros</span>
            </div>
            <div class="details">
              <ul>
                <li class="participants">
                  <span class="icon">👥</span> 8 participants
                </li>
                <li class="duration">
                  <span class="icon">⏱</span> 40 min
                </li>
              </ul>
              <div class="cta">
                <a href="/activities/25" class="btn btn-bold btn-primary link">Discover</a>
              </div>
            </div>
          </div>
      <% end %>
  </body>
</html>
